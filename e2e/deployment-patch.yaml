apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: debian-test
spec:
  template:
    spec:
      containers:
      - name: debian-test
        env:
        - name: ENVIRONMENT
          value: e2e
         
