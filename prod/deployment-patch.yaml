apiVersion: apps/v1
kind: Deployment
metadata:
  name: debian-test
spec:
  template:
    spec:
      containers:
      - name: debian-test
        env:
        - name: ENVIRONMENT
          value: production
          
